// Botão volta
container_principal.appendChild(aRead)

// Container Azul
    container_principal.appendChild(col_blue)

    // Formulario
        col_blue.appendChild(formCreate)

        // Div Titulo
            formCreate.appendChild(div1)
        
            // Title
                div1.appendChild(titleCreate)

        // Fim da Div Titulo
        
        // Div Nome
            formCreate.appendChild(div2)

            // Nome
                div2.appendChild(labelNome)
                div2.appendChild(inputNome)

        // Fim da Div Nome

        // Div Email
            formCreate.appendChild(div3)

            // Email
                div3.appendChild(labelEmail)
                div3.appendChild(inputEmail)

        // Fim da Div Email

        // Div Data de Nascimento
            formCreate.appendChild(div4)

                // Data de Nascimento
                    div4.appendChild(labelDT_Nascimento)
                    div4.appendChild(inputDT_Nascimento)   
        // Fim da Div Data de Nascimento

        // Div botão
                formCreate.appendChild(div5)

                // Botão Salvar
                    div5.appendChild(btnSalvar)
        // Fim da Div botão
    // Fim do Formulario

// Fim do Container Azul